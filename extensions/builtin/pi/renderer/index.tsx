import type { NativeExtension } from '../../../../src/extensions/extensionTypes';
import { piExtensionManifest } from '../manifest';

export const piExtension: NativeExtension = {
  manifest: piExtensionManifest,
};
